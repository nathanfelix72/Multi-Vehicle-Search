{
  "name": "multi-vehicle-search",
  "version": "1.0.0",
  "description": "API for searching vehicle storage locations",
  "scripts": {
    "build": "echo 'No build step required'"
  },
  "engines": {
    "node": ">=18.x"
  }
}